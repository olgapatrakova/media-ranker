<h2>User Summary: <%= @user.username %></h2>
<p>Joined site <%= @user.created_at.to_s.to_time.strftime('%B %e, %Y') %></p>

<section class="user-votes__container">
  <h3 class="user-votes__header">Votes</h3>
  <table class="table">
    <thead>
      <tr>
        <th>Media Title</th>
        <th>Created By</th>
        <th>Published</th>
        <th>Category</th>
        <th>Voted On</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</section>